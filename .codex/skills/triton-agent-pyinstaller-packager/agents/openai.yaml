interface:
  display_name: "Triton Agent PyInstaller Packager"
  short_description: "Package triton-agent into OS-specific PyInstaller bundles."
  default_prompt: "Package triton-agent for the current operating system with PyInstaller, include the skills tree, validate the executable, and summarize produced artifacts."
